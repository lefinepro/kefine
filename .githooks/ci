#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
NODE_CONTAINER_IMAGE="node:22-alpine"

run_frontend_check() {
  local command="$1"
  docker run --rm \
    -v "${PROJECT_ROOT}:/workspace" \
    -w /workspace \
    "${NODE_CONTAINER_IMAGE}" \
    sh -lc "corepack enable && yarn install && ${command}"
}

run_frontend_pipeline() {
  run_frontend_check "yarn lint"
  run_frontend_check "yarn check || (echo \"svelte-check had warnings, continuing\" && exit 0)"
  run_frontend_check "yarn test"
  run_frontend_check "yarn test:integration"
  run_frontend_check "yarn playwright install --with-deps chromium && yarn test:e2e"
  run_frontend_check "yarn build"
}

run_crater_smoke() {
  cd "${PROJECT_ROOT}"
  ./scripts/crater-smoke.sh
}

run_all() {
  run_frontend_pipeline
  run_crater_smoke
}

TARGET="${1:-all}"
case "${TARGET}" in
  lint)
    run_frontend_check "yarn lint"
    ;;
  check)
    run_frontend_check "yarn check || (echo \"svelte-check had warnings, continuing\" && exit 0)"
    ;;
  test)
    run_frontend_check "yarn test"
    ;;
  integration)
    run_frontend_check "yarn test:integration"
    ;;
  e2e)
    run_frontend_check "yarn playwright install --with-deps chromium && yarn test:e2e"
    ;;
  smoke)
    run_crater_smoke
    ;;
  all)
    run_all
    ;;
  *)
    echo "Unknown target: ${TARGET}" >&2
    echo "Use one of: lint, check, test, integration, e2e, smoke, all"
    exit 1
    ;;
esac
